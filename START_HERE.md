# 🚀 НАЧАЛО РАБОТЫ - ПОШАГОВЫЕ ИНСТРУКЦИИ

## ⏱️ ВРЕМЯ: 5-10 МИНУТ ДО ПОЛНОЙ ГОТОВНОСТИ

---

## ШАГОЛБНИЦА INSTALL & RUN (4 команды)

### 1️⃣ Установка (1 минута)

```bash
pip install -r requirements.txt && npm install
```

**Что это делает:**
- Устанавливает Python зависимости (Flask, requests, pytest и т.д.)
- Устанавливает Node.js зависимости (Cypress)

### 2️⃣ Запуск приложения (в отдельном терминале)

```bash
python src/app.py
```

**Что это делает:**
- Запускает Flask web сервер
- Приложение будет доступно на http://localhost:5000

### 3️⃣ Запуск юнит-тестов (30 секунд)

```bash
pytest tests/unit/ -v
```

**Ожидаемый результат:**
```
============================== 9 passed in 0.05s ===============================
✅ Все тесты прошли успешно!
```

### 4️⃣ Запуск UI-тестов (интерактивно)

```bash
npx cypress open
```

**Что это делает:**
- Открывает Cypress Test Runner
- Можно выбрать и запустить отдельные тесты
- Смотреть как тесты выполняются в реальном времени

---

## 📖 РЕКОМЕНДУЕМЫЙ ПОРЯДОК ЧТЕНИЯ

### 1. Быстрое ознакомление (10 минут)
```
1. README.md (основная информация)
2. Запустить приложение (python src/app.py)
3. Открыть http://localhost:5000 в браузере
4. Запустить тесты (pytest tests/unit/ -v)
```

### 2. Подробное изучение (30 минут)
```
1. TESTING_DOCUMENTATION.md (как работают тесты)
2. TEST_RUNNING_GUIDE.md (команды для запуска)
3. Открыть cypress/e2e/translator_critic.cy.js (UI-тесты)
4. Открыть tests/unit/test_app.py (юнит-тесты)
```

### 3. Практика (20 минут)
```
1. npx cypress open (смотреть UI-тесты в действии)
2. Изменить текст в app.py и посмотреть что упадет
3. Попробовать написать свой тест
4. Прочитать QA_TESTING_GUIDE.md (ручные тест-кейсы)
```

---

## 📁 ОСНОВНЫЕ ФАЙЛЫ

| Файл | Что это | Зачем читать |
|------|---------|-----------|
| **README.md** | Основная документация | Начните отсюда |
| **src/app.py** | Flask приложение | Посмотрите как работает функция call_llm |
| **src/templates/index.html** | Web интерфейс | Посмотрите форму и стили |
| **tests/unit/test_app.py** | Юнит-тесты (9 штук) | Научитесь писать тесты |
| **cypress/e2e/translator_critic.cy.js** | UI-тесты (5 штук) | Смотрите как писать Cypress тесты |
| **TESTING_DOCUMENTATION.md** | Полное объяснение | Подробное изучение тестирования |
| **QA_TESTING_GUIDE.md** | Тест-кейсы для ручного тестирования | Идеи для своих тестов |

---

## 🎯 БЫСТРЫЕ КОМАНДЫ

```bash
# Все в одной строке
pip install -r requirements.txt && npm install && python src/app.py &

# Потом в новом терминале
pytest tests/unit/ -v

# И в еще одном
npx cypress open
```

---

## ❓ ВОПРОСЫ И ОТВЕТЫ

**Q: Где находится приложение?**  
A: http://localhost:5000 (если запущен python src/app.py)

**Q: Как мне вводить API ключ?**  
A: В файл `.env` замените `your_api_key_here` на реальный ключ

**Q: Почему тесты используют моки вместо реального API?**  
A: Потому что тесты должны быть быстрыми и независимыми от внешних сервисов

**Q: Как я могу видеть Cypress тесты в действии?**  
A: Запустите `npx cypress open` и выберите тест

**Q: Все ли тесты написаны?**  
A: Да, 9 юнит-тестов + 5 UI-тестов = 14 тестов всего

**Q: Могу ли я добавить свои тесты?**  
A: Да! Смотрите примеры в test_app.py и translator_critic.cy.js

---

## 🚀 ПЕРВЫЕ ШАГИ

### Шаг 1: Убедитесь что установлены зависимости
```bash
pip --version      # Должно вывести версию Python
npm --version      # Должно вывести версию Node
```

### Шаг 2: Установите зависимости проекта
```bash
pip install -r requirements.txt
npm install
```

### Шаг 3: Запустите Flask приложение
```bash
python src/app.py
# Вы должны увидеть:
# Running on http://127.0.0.1:5000
```

### Шаг 4: Откройте браузер
```
http://localhost:5000
```

### Шаг 5: Запустите тесты
```bash
# Юнит-тесты
pytest tests/unit/ -v

# UI-тесты
npx cypress run
```

---

## 🎓 КАКИЕ НАВЫКИ ВЫ ПОЛУЧИТЕ

После работы с этим проектом вы будете знать:

✅ Как писать юнит-тесты на pytest  
✅ Как писать UI-тесты на Cypress  
✅ Как использовать моки (mock) для изолирования кода  
✅ Как работать с Flask приложением  
✅ Как работать с HTTP API запросами  
✅ Как обрабатывать ошибки при тестировании  
✅ Как писать документацию к тестам  
✅ Как использовать фикстуры (fixtures)  

---

## 📞 НУЖНА ПОМОЩЬ?

### Проблема: Не устанавливается pip пакет

**Решение:**
```bash
# Обновите pip
pip install --upgrade pip

# Потом попробуйте снова
pip install -r requirements.txt
```

### Проблема: pytest не находит модуль app

**Решение:**
```bash
# Убедитесь что находитесь в папке LLM
cd /workspaces/LLM

# Запустите тесты
pytest tests/unit/ -v
```

### Проблема: Cypress не открывается

**Решение:**
```bash
# Убедитесь что Flask приложение запущено
python src/app.py

# Потом в новом терминале
npx cypress open
```

### Проблема: Тесты падают

**Решение:**
1. Прочитайте сообщение об ошибке
2. Откройте файл теста и посмотрите что не так
3. Проверьте что приложение запущено
4. Посмотрите логи в консоли

---

## 💾 ВСЕ ФАЙЛЫ ПРОЕКТА

```
LLM/
├── 📖 ДОКУМЕНТАЦИЯ
│   ├── README.md                      (425 строк)
│   ├── TESTING_DOCUMENTATION.md       (675 строк)
│   ├── TEST_RUNNING_GUIDE.md          (340 строк)
│   ├── QA_TESTING_GUIDE.md            (250 строк)
│   ├── PROJECT_COMPLETION_REPORT.md   (330 строк)
│   ├── PROJECT_SUMMARY.md             (300 строк)
│   ├── TESTING_STATUS.md              (350 строк)
│   └── START_HERE.md                  (этот файл)
│
├── 🚀 ПРИЛОЖЕНИЕ
│   ├── src/
│   │   ├── app.py                     (450+ строк)
│   │   └── templates/
│   │       └── index.html             (300+ строк)
│   ├── requirements.txt
│   ├── .env                           (API ключ)
│   ├── package.json
│   └── cypress.config.js
│
├── 🧪 ТЕСТЫ
│   ├── tests/
│   │   ├── __init__.py
│   │   ├── conftest.py                (100+ строк, 3 фикстуры)
│   │   └── unit/
│   │       ├── __init__.py
│   │       └── test_app.py            (400+ строк, 9 тестов)
│   │
│   └── cypress/
│       ├── cypress.config.js
│       └── e2e/
│           └── translator_critic.cy.js (350+ строк, 5 тестов)
│
└── 🔧 КОНФИГ
    ├── .env                           (API ключ)
    ├── .gitignore                     (исключения)
    ├── package.json                   (NPM скрипты)
    └── requirements.txt               (Python зависимости)
```

---

## ✨ ГОТОВО!

Вы готовы начать работу с проектом!

**Последовательность:**
1. `pip install -r requirements.txt && npm install` (установка)
2. `python src/app.py` (запуск приложения)
3. Откройте http://localhost:5000
4. `pytest tests/unit/ -v` (запуск юнит-тестов)
5. `npx cypress open` (запуск UI-тестов)

**Наслаждайтесь!** 🎉

---

**Создано:** 9 декабря 2024  
**Версия:** 1.0  
**Статус:** ✅ ГОТОВО  
