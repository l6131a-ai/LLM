# 🌍 AI Translator & Critic - Flask Web Application

## 📋 Описание проекта

**AI Translator & Critic** - это веб-приложение на Flask, которое позволяет:
- 🔤 Переводить текст на различные языки используя AI модели
- ⚖️ Оценивать качество перевода автоматически (LLM-as-a-Judge)
- 📊 Получать подробный анализ перевода от языковой модели

## 🏗️ Архитектура проекта

```
LLM/
├── src/
│   ├── app.py                 # Основная логика Flask приложения
│   └── templates/
│       └── index.html         # HTML шаблон с интерфейсом
├── requirements.txt           # Python зависимости
├── .env                       # Переменные окружения (API ключ)
├── .gitignore                 # Исключение файлов из Git
└── README.md                  # Этот файл
```

## 🛠️ Технологический стек

| Компонент | Версия | Назначение |
|-----------|--------|-----------|
| **Flask** | 2.3.2 | Web framework для создания маршрутов |
| **requests** | 2.31.0 | HTTP клиент для запросов к API |
| **python-dotenv** | 1.0.0 | Загрузка переменных окружения |
| **Bootstrap 5** | 5.3.0 | CSS framework для стилей (CDN) |

## 🔐 API Integration

### Endpoint
```
https://api.mentorpiece.org/v1/process-ai-request
```

### Authentication
```
Headers:
  Authorization: Bearer {API_KEY}
  Content-Type: application/json
```

### Request Format
```json
{
  "model_name": "claude-sonnet-4-5-20250929",
  "prompt": "Your prompt here"
}
```

### Response Format
```json
{
  "response": "API response text"
}
```

## 📦 Установка и запуск

### Шаг 1: Клонирование репозитория
```bash
git clone https://github.com/l6131a-ai/LLM.git
cd LLM
```

### Шаг 2: Создание виртуального окружения
```bash
# На Unix/Mac
python3 -m venv venv
source venv/bin/activate

# На Windows
python -m venv venv
venv\Scripts\activate
```

### Шаг 3: Установка зависимостей
```bash
pip install -r requirements.txt
```

### Шаг 4: Конфигурация API ключа
1. Откройте файл `.env`
2. Замените `your_api_key_here` на ваш реальный API ключ:
```env
MENTORPIECE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx
```

### Шаг 5: Запуск приложения
```bash
# Убедитесь, что вы находитесь в директории проекта
python src/app.py
```

Приложение будет доступно по адресу: **http://localhost:5000**

## 🧑‍💻 Использование приложения

### 1. Открытие главной страницы
Перейдите на `http://localhost:5000` - вы увидите форму приложения

### 2. Ввод текста для перевода
- Введите текст в поле "Введите текст для перевода"
- Выберите целевой язык из выпадающего списка (Английский, Французский, Немецкий)

### 3. Запуск перевода
Нажмите кнопку "🚀 Перевести" - приложение выполнит:
- **Шаг 1**: Перевод текста используя модель `Qwen/Qwen3-VL-30B-A3B-Instruct`
- **Шаг 2**: Оценка качества перевода используя модель `claude-sonnet-4-5-20250929`

### 4. Просмотр результатов
На странице отобразятся:
- 📌 Исходный текст
- 🌐 Переведенный текст
- ⚖️ Оценка качества с аргументацией

## 🔍 Для QA специалистов: Гайд по тестированию

### Функциональное тестирование

#### Test Case 1: Валидный перевод на английский
```
Предусловие: API ключ установлен
Шаг 1: Ввести текст: "Привет, мир!"
Шаг 2: Выбрать язык: "Английский"
Шаг 3: Нажать "Перевести"
Ожидаемый результат: Отобразятся переведенный текст и оценка качества
```

#### Test Case 2: Пустое поле ввода
```
Предусловие: Форма открыта
Шаг 1: Оставить поле "Текст" пустым
Шаг 2: Нажать "Перевести"
Ожидаемый результат: Ошибка "Пожалуйста, введите текст"
```

#### Test Case 3: Очень короткий текст
```
Шаг 1: Ввести текст: "Hi"
Шаг 2: Нажать "Перевести"
Ожидаемый результат: Предупреждение "Текст очень короткий"
```

### Тестирование обработки ошибок

#### Error Test 1: Отсутствует API ключ
```
Предусловие: MENTORPIECE_API_KEY не установлен в .env
Шаг 1: Ввести текст и выбрать язык
Шаг 2: Нажать "Перевести"
Ожидаемый результат: Ошибка "API ключ не загружен"
```

#### Error Test 2: Неверный API ключ
```
Предусловие: MENTORPIECE_API_KEY = "invalid_key"
Шаг 1: Ввести текст и выбрать язык
Шаг 2: Нажать "Перевести"
Ожидаемый результат: Ошибка 401 (Unauthorized)
```

#### Error Test 3: Недоступный сервер API
```
Предусловие: Интернет отключен или API сервер недоступен
Шаг 1: Ввести текст
Шаг 2: Нажать "Перевести"
Ожидаемый результат: Ошибка "Таймаут при подключении" или "Не удалось подключиться"
```

### Performance Testing

```
Критерий: Время ответа для перевода < 30 секунд
Критерий: Время ответа для оценки < 30 секунд
Критерий: Приложение не падает при одновременных запросах
```

### Логирование и отладка

Все ошибки логируются в консоль приложения. Ищите сообщения:
```
⚠️  API вернул ошибку: 401
⚠️  Ошибка: Таймаут при подключении к серверу
⚠️  Ошибка: Сервер вернул некорректный JSON
```

## 🐛 Решение распространенных проблем

### Проблема: "API ключ не загружен"
**Решение:**
1. Проверьте, что файл `.env` находится в корне проекта (рядом с `app.py`)
2. Убедитесь, что переменная названа точно: `MENTORPIECE_API_KEY`
3. Проверьте, что ключ не пуст: `MENTORPIECE_API_KEY=sk-xxx`
4. Перезагрузите приложение после изменения `.env`

### Проблема: "Ошибка 401 Unauthorized"
**Решение:**
1. Проверьте, что вы используете правильный API ключ
2. Убедитесь, что ключ еще активен и не истек
3. Проверьте наличие пробелов в начале/конце ключа

### Проблема: "Таймаут при подключении"
**Решение:**
1. Проверьте интернет-соединение
2. Убедитесь, что API сервер доступен
3. Проверьте URL endpoint: `https://api.mentorpiece.org/v1/process-ai-request`
4. Попробуйте увеличить таймаут в коде (по умолчанию 30 сек)

### Проблема: Flask говорит "Address already in use"
**Решение:**
```bash
# Найти процесс на порту 5000
lsof -i :5000

# Убить процесс (замените PID на реальный ID)
kill -9 <PID>

# Или использовать другой порт
python src/app.py --port 5001
```

## 📝 Структура кода (для QA)

### app.py - Основные компоненты

#### 1. Функция `call_llm(model_name, messages)`
```python
def call_llm(model_name, messages):
    """Отправляет запрос к LLM API"""
    # Проверка API ключа
    # Подготовка заголовков
    # Отправка POST запроса
    # Обработка ошибок (5 типов исключений)
    # Возврат результата
```

Обработанные ошибки:
- `requests.exceptions.Timeout` - таймаут
- `requests.exceptions.ConnectionError` - ошибка подключения
- `requests.exceptions.RequestException` - общие ошибки запроса
- `ValueError` - ошибка парсинга JSON
- `Exception` - неожиданные ошибки

#### 2. Route `GET /`
Отображает форму ввода с пустыми полями

#### 3. Route `POST /`
Процесс обработки:
1. Получает текст и язык из формы
2. Вызывает `call_llm` для перевода
3. Вызывает `call_llm` для оценки качества
4. Возвращает результаты на страницу

## 🚀 Production deployment

Для развертывания в production:

1. **Установите production WSGI сервер:**
```bash
pip install gunicorn
```

2. **Отключите debug режим:**
Измените в `app.py`:
```python
app.run(host="0.0.0.0", port=5000, debug=False)
```

3. **Запустите с gunicorn:**
```bash
gunicorn -w 4 -b 0.0.0.0:5000 src.app:app
```

4. **Используйте переменные окружения на сервере:**
Не коммитьте `.env` файл, установите переменные на сервере через:
- Docker environment variables
- Kubernetes secrets
- Cloud provider configuration

## 📚 Дополнительные ресурсы

- [Flask Documentation](https://flask.palletsprojects.com/)
- [Requests Library](https://requests.readthedocs.io/)
- [Bootstrap 5 Docs](https://getbootstrap.com/docs/5.3/)
- [Python-dotenv](https://python-dotenv.readthedocs.io/)

## 📄 Лицензия

MIT License - свободно используйте в личных и коммерческих проектах

## 👨‍💼 Поддержка

Если у вас есть вопросы:
1. Проверьте раздел "Решение распространенных проблем"
2. Посмотрите консоль приложения на предмет ошибок
3. Создайте Issue на GitHub

---

**Автор:** AI Backend Developer  
**Дата создания:** 2024  
**Версия:** 1.0