// ===========================================================================================
// cypress.config.js - Конфигурация Cypress
// ===========================================================================================
// Этот файл содержит настройки для запуска Cypress тестов
// ===========================================================================================

const { defineConfig } = require('cypress');

module.exports = defineConfig({
  // Настройки для всех браузеров
  e2e: {
    // Базовый URL приложения
    // Когда вы используете cy.visit('/'), Cypress использует этот URL
    baseUrl: 'http://localhost:5000',
    
    // Папка где хранятся е2е тесты
    specPattern: 'cypress/e2e/**/*.cy.js',
    
    // Поддерживаемые браузеры
    // Вы можете запустить тесты в разных браузерах
    // npx cypress run --browser chrome
    // npx cypress run --browser firefox
    // npx cypress run --browser edge
    
    // Время ожидания для команд Cypress (в миллисекундах)
    defaultCommandTimeout: 10000, // 10 сек
    
    // Время ожидания для cy.visit
    pageLoadTimeout: 30000, // 30 сек
    
    // Время ожидания для сетевых запросов
    requestTimeout: 30000, // 30 сек
    
    // Размер видео (если вы записываете тесты)
    videoRecording: false, // Отключено, чтобы не замедлять тесты
  },
  
  // Общие настройки
  viewportWidth: 1280,   // Ширина браузера при запуске тестов
  viewportHeight: 720,   // Высота браузера при запуске тестов
});
