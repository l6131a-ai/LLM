# ===========================================================================================
# requirements.txt - Зависимости проекта
# ===========================================================================================
# Этот файл содержит список Python пакетов, необходимых для запуска приложения.
# Установка: pip install -r requirements.txt
# ===========================================================================================

# ОСНОВНЫЕ ЗАВИСИМОСТИ ПРИЛОЖЕНИЯ
# ================================

# Flask - веб-фреймворк для создания веб-приложений на Python
# Используется для: создания маршрутов, обработки запросов, рендеринга шаблонов
Flask==2.3.2

# requests - библиотека для выполнения HTTP запросов
# Используется для: отправки POST запросов к API endpoint
requests==2.31.0

# python-dotenv - загрузка переменных окружения из файла .env
# Используется для: безопасной загрузки API ключа без хранения в коде
python-dotenv==1.0.0

# Werkzeug - утилиты для WSGI приложений (используется Flask)
# Автоматически устанавливается с Flask
Werkzeug==2.3.6

# Jinja2 - шаблонизатор (используется Flask для рендеринга HTML)
# Автоматически устанавливается с Flask
Jinja2==3.1.2

# ЗАВИСИМОСТИ ДЛЯ ТЕСТИРОВАНИЯ
# ============================

# pytest - фреймворк для написания и запуска юнит-тестов
# Используется для: организации тестов, запуска их и создания отчетов
pytest==7.4.3

# pytest-cov - плагин для pytest для измерения покрытия кода тестами
# Используется для: создания отчета о том какие части кода протестированы
pytest-cov==4.1.0

# pytest-mock - плагин для pytest для удобного мокирования
# Используется для: замены реальных функций на mock-объекты в тестах
pytest-mock==3.12.0

# Дополнительные опциональные зависимости для метрик
# langdetect - используется для проверки языка в unit-тестах
langdetect>=1.0.9

# scikit-learn - опционально для более точной семантической схожести (TF-IDF + cosine)
# Рекомендуется установить вручную при необходимости
# scikit-learn>=1.2.0
